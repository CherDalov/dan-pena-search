# Python
__pycache__/
*.pyc
.venv/
venv/

# SQLite index (regenerated by ingest.py; cached in CI, not committed)
search.db
search.db-*

# logs
*.log

# auth / secrets (YouTube session cookies — never commit)
cookies.txt
*cookies*.txt

# env / secrets (Turso credentials, etc. — never commit)
.env
.env.*
!.env.example

# Node / web
node_modules/
web/node_modules/
web/dist/
web/.vite/

# generated locally as the Turso load source; served from Turso, not the deploy
web/public/index.json

# Vercel
.vercel/

# OS
.DS_Store
Thumbs.db

.vercel

.vercel
